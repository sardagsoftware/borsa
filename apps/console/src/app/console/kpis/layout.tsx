import React from "react";

export const metadata = {
  title: "KPIs & Telemetry - Season 2 | Ailydian Console",
  description: "Real-time KPI monitoring for Echo of Sardis Season 2",
};

export default function KpisLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
