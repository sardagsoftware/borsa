# Realtime WebSocket Configuration
# =================================

# WebSocket/SSE secret for HMAC authentication
# IMPORTANT: Change this in production! Use Vault/KMS for rotation
WS_HMAC_SECRET=super-secret-from-vault-rotate-24h
NEXT_PUBLIC_RT_SECRET=dev-secret-change-in-production

# API Base URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:3100

# Default RBAC scopes (comma-separated)
RBAC_DEFAULT_SCOPES=ops.admin,liveops.admin,economy.admin

# Server Configuration
PORT=3100
NODE_ENV=development

# Security Settings
# -----------------

# HMAC replay window (seconds)
HMAC_REPLAY_WINDOW=300

# Max WebSocket connections per client
MAX_WS_CONNECTIONS=10

# Heartbeat interval (ms)
HEARTBEAT_INTERVAL=15000

# Reconnect settings
RECONNECT_DELAY=2000
MAX_RECONNECT_ATTEMPTS=10

# Backpressure settings
MAX_BUFFER_SIZE=1048576
MAX_QUEUE_SIZE=200
FLUSH_INTERVAL=50

# Compliance
# ----------

# Data retention (days)
DATA_RETENTION_DAYS=90

# Logging level
LOG_LEVEL=info

# Attested logs (enable for production)
ATTESTED_LOGS=false

# KVKK/GDPR compliance mode
COMPLIANCE_MODE=strict
