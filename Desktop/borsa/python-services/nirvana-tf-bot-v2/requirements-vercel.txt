# Nirvana TF Bot v2 - Vercel Optimized Dependencies
# Lightweight version without heavy dependencies for serverless

# Core ML (lightweight versions)
numpy>=1.24.0,<2.0.0
pandas>=2.0.0

# Technical Analysis (no TA-Lib for Vercel)
# ta>=0.11.0  # Commented out - use manual calculations

# API & Web
fastapi>=0.110.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
httpx>=0.27.0
orjson>=3.9.0
python-dateutil>=2.8.0

# Note: TensorFlow is too large for Vercel serverless (50MB limit)
# API will serve without model, using mock predictions
# For full TensorFlow inference, use Docker deployment