# Development files
node_modules
.env
.env.local
.env.*.local

# Logs
*.log
logs
server.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test files
test-results
playwright-report
*.spec.js
*.test.js
*.spec.ts
*.test.ts

# TypeScript API files (not compiled, causes build errors)
api/**/*.ts
!api/**/*.d.ts

# IDE
.vscode
.idea
.DS_Store
*.swp
*.swo

# Documentation
*.md
!README.md
docs

# Backup files
*backup*
*BACKUP*
*.bak
*.tar.gz

# Azure services (local only)
azure-services
monitoring

# Apps (using separate repos)
apps

# API endpoints (causing bundle size > 250MB due to node_modules)
# Temporarily disabled for translation-only deployment
api

# Packages (SDK development, not for production)
packages

# Services (microservices development, not for production)
services

# Multi-language libraries (local only)
multi-language-libraries

# Infrastructure (local only)
infra

# Database (local only)
database

# Backend (local only, if separate)
backend

# Security - REQUIRED by API endpoints!
# security

# AI integrations (local only, if not needed)
ai-integrations

# Large files
*.tar.gz
*.zip
*.rar

# Heavy dependencies (reduce bundle size)
node_modules/puppeteer
node_modules/canvas
node_modules/@azure/cognitiveservices-*
node_modules/@azure/arm-*
node_modules/onnxruntime-node
node_modules/sharp
node_modules/neo4j-driver
node_modules/weaviate-client
